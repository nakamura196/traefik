services:
  traefik:
    image: traefik:v2.11
    container_name: traefik
    restart: always
    depends_on:
      - crowdsec
    ports:
      - 443:443
      - 80:80
    networks:
      - traefik-network
    environment:
      TZ: Asia/Tokyo
    labels:
      traefik.enable: true

      # Crowdsec Bouncer middleware (global)
      traefik.http.middlewares.crowdsec.plugin.bouncer.enabled: true
      traefik.http.middlewares.crowdsec.plugin.bouncer.crowdsecLapiKey: ${CROWDSEC_BOUNCER_KEY}
      traefik.http.middlewares.crowdsec.plugin.bouncer.crowdsecLapiHost: crowdsec:8080

    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./letsencrypt:/letsencrypt
      - ./traefik.yml:/etc/traefik/traefik.yml:ro
      - ./dynamic:/etc/traefik/dynamic:ro
      - ./plugins-local:/plugins-local:ro
      - ./logs:/var/log/traefik

  crowdsec:
    image: crowdsecurity/crowdsec:latest
    container_name: crowdsec
    restart: always
    environment:
      TZ: Asia/Tokyo
      COLLECTIONS: "crowdsecurity/traefik crowdsecurity/http-cve crowdsecurity/base-http-scenarios"
      # Disable overly aggressive scenarios that may ban normal users
      DISABLE_SCENARIOS: "crowdsecurity/http-crawl-non_statics crowdsecurity/http-probing"
      GID: "1000"
    volumes:
      - crowdsec-db:/var/lib/crowdsec/data/
      - crowdsec-config:/etc/crowdsec/
      - ./crowdsec/acquis.yaml:/etc/crowdsec/acquis.yaml:ro
      - ./crowdsec/parsers:/etc/crowdsec/parsers/s02-enrich/custom:ro
      - ./logs:/var/log/traefik:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - traefik-network

networks:
  traefik-network:
    external: true

volumes:
  crowdsec-db:
  crowdsec-config:
